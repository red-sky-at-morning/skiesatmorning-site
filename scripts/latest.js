// Gets whatever page is marked as 'frontpage' from meta.json, and loads the content into the 'whatever' element in the page its on

document.addEventListener("DOMContentLoaded", function () {
    let frontpage = document.getElementById("page-container")
    
});

